# GenAI

```
pip install ipykernel
```


```
python -m venv env

.\env\Scripts\activate

pip list

git status

git add .

git commit -m "folder structure updated"

git remote add origin https://github.com/Anshuman7t/GenAI.git

git push -f origin main
   
pip install -r requirements.txt 
```